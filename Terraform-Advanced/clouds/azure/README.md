# Azure Entry Point

This directory is the Azure-side platform entrypoint for the multicloud scaffold.

Use it to wire:

- VNet and subnet modules
- AKS and node pools
- Entra ID / RBAC patterns
- Azure Monitor and platform observability

The files here are intentionally lightweight so you can add real infrastructure module-by-module.
