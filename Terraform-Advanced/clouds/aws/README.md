# AWS Entry Point

This directory is the AWS-side platform entrypoint for the multicloud scaffold.

Use it to wire:

- VPC and subnet modules
- EKS and managed node groups
- IAM / security patterns
- CloudWatch and platform observability

The files here are intentionally lightweight so you can add real infrastructure module-by-module.
