Tencent Cloud placeholder for additional services.

Add resources here that you want to provision in Tencent Cloud (e.g., CDN, object storage, DNS, other region-specific services). Include a `main.tf` with `tencentcloud` provider and `variables.tf` for credentials and region.

Keep secrets out of source control and use a secure backend for state.
